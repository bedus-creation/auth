services:

  auth:
    build:
      context: ../..
      dockerfile: tenants/auth/Dockerfile
    ports:
      - "7700:7700"
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage   # persist the SQLite file across restarts
    restart: unless-stopped
